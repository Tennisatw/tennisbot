diff --git a/src/load_agent.py b/src/load_agent.py
--- a/src/load_agent.py
+++ b/src/load_agent.py
@@ -47,6 +47,11 @@
     )
     return agent
 
+
+def _log_handoff(ctx, input_data, *, to_agent) -> None:
+    """Log SDK handoff event."""
+    logger.log(f"handoff from={getattr(ctx.agent, 'name', '-')} to={getattr(to_agent, 'name', '-')}")
+
+
 def load_sub_agents(handoffs):
     # Dynamically load sub-agents from agents/sub_agents/
 
     ROOT_PATH = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
@@ -55,11 +60,7 @@
     handoff_tools = [
         handoff(
             agent=a,
-            on_handoff=lambda ctx, input_data, a=a: logger.log(
-                f"handoff from={getattr(ctx.agent, 'name', '-')} to={getattr(a, 'name', '-')}"
-            ),
+            on_handoff=lambda ctx, input_data, a=a: _log_handoff(ctx, input_data, to_agent=a),
         )
         for a in handoffs
     ]
