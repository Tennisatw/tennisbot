diff --git a/src/tools/read_file.py b/src/tools/read_file.py
--- a/src/tools/read_file.py
+++ b/src/tools/read_file.py
@@ -1,4 +1,6 @@
 from agents import function_tool
+from src.logger import logger
+
 
 @function_tool
 async def read_file(
@@ -16,19 +18,26 @@
             "error": str | None,
         }
     """
     print(f"Reading file: {path}")
+    logger.log("tool.read_file.input", path=path)
     try:
         with open(path, "r", encoding="utf-8", errors="ignore") as f:
             contents = f.read()
+
+            preview = contents.replace("\r\n", "\\n").replace("\n", "\\n")
+            if len(preview) > 50:
+                preview = preview[:50]
+            logger.log("tool.read_file.output", success=True, content_preview=preview, content_len=len(contents))
+
             return {
                 "success": True,
                 "content": contents,
                 "error": None,
             }
     except Exception as e:
+        err = str(e)
+        logger.log("tool.read_file.output", success=False, error=err)
         return {
             "success": False,
             "content": None,
-            "error": str(e),
+            "error": err,
         }
