diff --git a/src/logger.py b/src/logger.py
--- a/src/logger.py
+++ b/src/logger.py
@@ -100,6 +100,7 @@
 
         logger.addHandler(file_handler)
         logger.addHandler(console_handler)
         logger.addFilter(_DefaultExtraFilter())
+        logger._tennisbot_log_date = datetime.now().strftime("%Y-%m-%d")
         return logger
+
+    def _ensure_today_file(self) -> None:
+        """Re-setup logger if date changed."""
+
+        lg = logging.getLogger(self.name)
+        today = datetime.now().strftime("%Y-%m-%d")
+        if getattr(lg, "_tennisbot_log_date", None) == today:
+            return
+        self.setup()
@@ -107,6 +118,7 @@
     def log(self, message) -> None:
         """Log a message."""
+        self._ensure_today_file()
         logging.getLogger(self.name).info(message)
