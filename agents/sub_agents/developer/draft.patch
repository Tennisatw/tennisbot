diff --git a/web/frontend/src/app.css b/web/frontend/src/app.css
--- a/web/frontend/src/app.css
+++ b/web/frontend/src/app.css
@@ -1,21 +1,21 @@
 @tailwind base;
 @tailwind components;
 @tailwind utilities;
 
 @layer components {
   /* Reduce vertical spacing for markdown blocks rendered by Tailwind Typography. */
   .prose :where(p, ul, ol, li, pre, blockquote, h1, h2, h3, h4, h5, h6) {
-    margin-block: 0em;
+    margin-block: 0.35em;
   }
 
   /* Wrap long code blocks on mobile to avoid horizontal scrolling. */
   .prose pre {
     white-space: pre-wrap;
     word-break: break-word;
     overflow-x: hidden;
   }
 
   .prose code {
     white-space: pre-wrap;
     word-break: break-word;
   }
 }
