diff --git a/web/frontend/src/App.svelte b/web/frontend/src/App.svelte
--- a/web/frontend/src/App.svelte
+++ b/web/frontend/src/App.svelte
@@ -142,9 +142,9 @@
         >
           {#if m.role === 'assistant'}
-            <div class="prose prose-base max-w-none">{@html renderMarkdown(m.text)}</div>
+            <div class="prose prose-base leading-snug max-w-none">{@html renderMarkdown(m.text)}</div>
           {:else}
-            <div class="prose prose-base prose-invert max-w-none">{@html renderMarkdown(m.text)}</div>{m.role === 'user' && m.status === 'pending' ? ' (sending...)' : ''}
+            <div class="prose prose-base prose-invert leading-snug max-w-none">{@html renderMarkdown(m.text)}</div>{m.role === 'user' && m.status === 'pending' ? ' (sending...)' : ''}
           {/if}
         </div>
       </div>
