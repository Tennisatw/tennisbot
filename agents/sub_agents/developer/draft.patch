diff --git a/src/tools/draft_patch.py b/src/tools/draft_patch.py
--- a/src/tools/draft_patch.py
+++ b/src/tools/draft_patch.py
@@ -51,7 +51,7 @@
 
     proc = subprocess.run(
-        [git_path, "apply", "--check", "--ignore-whitespace", "--recount", f"-p{path_strip}", str(draft_path)],
+        [git_path, "apply", "--check", "--recount", f"-p{path_strip}", str(draft_path)],
         text=True,
         encoding="utf-8",
         capture_output=True,
         check=False,
         timeout=60,
     )
